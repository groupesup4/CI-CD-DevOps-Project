name: CI/CD Pipeline

on:
  push:
    branches:
      - dev3
  pull_request:
    branches:
      - dev3

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

